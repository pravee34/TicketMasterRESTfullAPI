package com.ticket.master.constants;

/**
 * @enum variables 
 *
 */
public enum TicketTableColumn {
	AVAILABLE_SEATS, ROW_NUMBER, MEMBER_ID, EVENT_ID, ROW_COUNT, COLUMN_COUNT, EMAIL_ADDRESS, SEAT_NUMBER
}